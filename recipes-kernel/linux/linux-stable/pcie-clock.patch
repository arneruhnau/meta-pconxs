diff --git a/arch/arm/boot/dts/imx6qdl.dtsi b/arch/arm/boot/dts/imx6qdl.dtsi
index ce05991..8474257 100644
--- a/arch/arm/boot/dts/imx6qdl.dtsi
+++ b/arch/arm/boot/dts/imx6qdl.dtsi
@@ -149,8 +149,8 @@
 			                <0 0 0 2 &intc GIC_SPI 122 IRQ_TYPE_LEVEL_HIGH>,
 			                <0 0 0 3 &intc GIC_SPI 121 IRQ_TYPE_LEVEL_HIGH>,
 			                <0 0 0 4 &intc GIC_SPI 120 IRQ_TYPE_LEVEL_HIGH>;
-			clocks = <&clks 144>, <&clks 206>, <&clks 189>;
-			clock-names = "pcie", "pcie_bus", "pcie_phy";
+			clocks = <&clks 144>, <&clks 206>, <&clks 189>, <&clks 187>;
+			clock-names = "pcie", "pcie_bus", "pcie_phy", "pcie_sata";
 			status = "disabled";
 		};
 
diff --git a/drivers/pci/host/pci-imx6.c b/drivers/pci/host/pci-imx6.c
index a568efa..e634a9f 100644
--- a/drivers/pci/host/pci-imx6.c
+++ b/drivers/pci/host/pci-imx6.c
@@ -35,6 +35,7 @@ struct imx6_pcie {
 	int			reset_gpio;
 	struct clk		*pcie_bus;
 	struct clk		*pcie_phy;
+	struct clk		*pcie_sata;
 	struct clk		*pcie;
 	struct pcie_port	pp;
 	struct regmap		*iomuxc_gpr;
@@ -239,6 +240,12 @@ static int imx6_pcie_deassert_core_reset(struct pcie_port *pp)
 		goto err_pcie_phy;
 	}
 
+	ret = clk_prepare_enable(imx6_pcie->pcie_sata);
+	if (ret) {
+		dev_err(pp->dev, "unable to enable pcie_sata clock\n");
+		goto err_pcie_sata;
+	}
+
 	ret = clk_prepare_enable(imx6_pcie->pcie_bus);
 	if (ret) {
 		dev_err(pp->dev, "unable to enable pcie_bus clock\n");
@@ -265,6 +272,8 @@ static int imx6_pcie_deassert_core_reset(struct pcie_port *pp)
 err_pcie:
 	clk_disable_unprepare(imx6_pcie->pcie_bus);
 err_pcie_bus:
+	clk_disable_unprepare(imx6_pcie->pcie_sata);
+err_pcie_sata:
 	clk_disable_unprepare(imx6_pcie->pcie_phy);
 err_pcie_phy:
 	return ret;
@@ -573,6 +582,13 @@ static int __init imx6_pcie_probe(struct platform_device *pdev)
 		return PTR_ERR(imx6_pcie->pcie);
 	}
 
+	imx6_pcie->pcie_sata = devm_clk_get(&pdev->dev, "pcie_sata");
+	if (IS_ERR(imx6_pcie->pcie_sata)) {
+		dev_err(&pdev->dev,
+			"pcie_sata clock source missing or invalid\n");
+		return PTR_ERR(imx6_pcie->pcie_sata);
+	}
+
 	/* Grab GPR config register range */
 	imx6_pcie->iomuxc_gpr =
 		 syscon_regmap_lookup_by_compatible("fsl,imx6q-iomuxc-gpr");
-- 
1.8.5.5

